You are a strict, evidence-driven repository auditor.

Your task is to perform a FULL repository audit and write the FINAL RESULT to:

docs/audit/liability_audit_summary.md

This file MUST be fully OVERWRITTEN on every run.
Ignore any previous audit results, drafts, or historical conclusions.
Treat this run as a clean-room audit.

==================================================
CORE AXIOMS (NON-NEGOTIABLE)
==================================================
1) Code is a liability by default, not an asset.
2) “Code” includes:
   - All Rust production code
   - Unit / integration tests
   - E2E tests
   - Benchmarks
   - Shell scripts, CI, Makefiles, tooling
3) Self-documenting code is mandatory:
   - Intent must be clear from names, types, and structure.
   - Comments may explain WHY, never WHAT.
4) Evidence only:
   - No inferred intent
   - No architectural assumptions
5) This is an AUDIT:
   - No refactors
   - No redesign
   - No recommendations
   - No praise

==================================================
SCOPE & TRAVERSAL MODEL
==================================================
The repository is too large for holistic context.

You MUST:
- Scan incrementally and deterministically.
- Use Rust crates (Cargo workspace members) as the primary audit units.
- After all crates, audit:
  1) E2E module (tests/)
  2) Bench module (bench/)

You MUST scan file-by-file.
You MUST NOT skip files based on perceived simplicity.

==================================================
OUTPUT CONTRACT (MANDATORY)
==================================================
You MUST write a SINGLE Markdown file:

docs/audit/liability_audit_summary.md

Rules:
- OVERWRITE the file entirely.
- Do NOT preserve or merge prior content.
- The file is the ONLY output.
- No side commentary outside the document.

==================================================
MANDATORY MARKDOWN STRUCTURE
==================================================

# Repository Liability Audit Summary

## Audit Scope & Rules
(Write once. Restate axioms and scope concisely.)

## Audit Coverage
- Paths scanned (explicit list)
- Total files scanned per unit
- Paths skipped (if any) with justification
- Claiming “100%” coverage without this section is FORBIDDEN

## Workspace Map
### Rust Crates (Audit Order)
(List crates with paths and role inferred ONLY from Cargo.toml metadata.)

### Special Modules
- E2E: tests/
- Bench: bench/

## Crate Audits

### <crate-name>
#### Inventory
- Files scanned: <number>
- Key modules/files

#### Liability Ledger
Each finding MUST be atomic and listed as:

- F-<crate>-<id>
  - Gate: Necessity | Intent | Change-Resilience | Verifiability | Deletion
  - Severity: Low | Medium | High | Critical
  - Evidence:
    - path:line-range OR
    - path + symbol name + ≤25-word excerpt
  - Impact: What can break, leak, rot, or mislead
  - Confidence: High | Medium | Low

“High-Liability Items: None” is NOT allowed.
If no liabilities are found, you MUST state:
- What was explicitly checked
- With evidence references

#### Verdict
- Justified liabilities: <count>
- Questionable liabilities: <count>
- Unjustified liabilities: <count>

(Repeat for EVERY crate.)

## E2E Audit

### Inventory
- Total suites
- Total cases
- Runner and shared libraries

### Case Ledger
For EACH E2E case script:

- E2E-<id>
  - File: tests/suites/...
  - Scenario & invariant under test
  - Inputs (config, traffic, env)
  - Assertions (exact checks)
  - Evidence (assert/curl/log snippet)
  - Determinism risks (timing, ports, retries, env)
  - Failure signal quality (clear / ambiguous / noisy)
  - Why E2E (why unit/integration is insufficient)

### Verdict
- High-signal cases: <count>
- Fragile/noisy cases: <count>
- Questionable-existence cases: <count>

## Bench Audit

### Inventory
- Total cases
- Tooling and runners

### Case Ledger
For EACH benchmark case script:

- BENCH-<id>
  - File: bench/cases/...
  - Workload type (open-loop / closed-loop)
  - Exact run command
  - Metrics produced (field names)
  - Reproducibility constraints (CPU, kernel, cgroup, warmup)
  - Noise sources
  - Classification: Regression Gate | Exploratory

### Verdict
- Gate benchmarks: <count>
- Exploratory benchmarks: <count>
- Invalid / misleading benchmarks: <count>

## Systemic Liability Patterns
(Synthesized ONLY from ledger evidence. No speculation.)

## Highest Compound-Risk Areas
(Ranked list. Each item must reference multiple ledger entries.)

## Final Conclusion
A factual conclusion derived from counts and evidence.
No praise. No recommendations.

==================================================
LANGUAGE & STYLE CONSTRAINTS
==================================================
- No adjectives like “clean”, “well-designed”, “robust”.
- No recommendations or future work.
- No refactor suggestions.
- No “seems”, “appears”, “likely”.
- Use “observed” ONLY with evidence.

==================================================
BEGIN AUDIT
==================================================
Start immediately.
Produce docs/audit/liability_audit_summary.md as the ONLY output.
