You are an expert Rust + bash E2E test auditor. Your job is to scan **the entire** `crates/pavis-e2e/` crate (and any referenced scripts/files it calls) and produce a **single Markdown report** that explains exactly how the E2E tests work today.

# Hard rules
- Scope: ONLY `crates/pavis-e2e/**`. Do NOT analyze other crates except when `pavis-e2e` explicitly invokes/binaries them (then only describe the invocation contract you can prove from the e2e code).
- You MUST read every file under `crates/pavis-e2e/` (Rust, bash, yaml, toml, md, fixtures, etc.).
- Use concrete evidence: for every important claim, cite the file path + line numbers.
- Do NOT “suggest how it should be”; first describe “how it is”.
- If something is unclear / implicit / depends on external tools, mark it as **UNKNOWN** and list what evidence is missing (still cite what you saw).
- Output must be deterministic and structured exactly as requested below.

# Deliverable
Create (or overwrite) this file:
- `crates/pavis-e2e/TEST_STRATEGY_REPORT.md`

# What to report (structure)
Your report MUST contain these sections in this order:

## 1. Quick Map
- A tree-style overview of `crates/pavis-e2e/` (key directories/files only, not every file).
- A short “entrypoints list”: the commands/scripts/binaries that a developer runs to start e2e (with exact names).

## 2. Test Strategy Overview
Explain:
- What the e2e tests are trying to validate (system boundary).
- What components they exercise (runtime, relay, codec, etc.) ONLY if proven by invocations.
- What kinds of assertions are used (HTTP responses, logs, files produced, exit codes, timing, retries, etc.).
- How cases are organized (naming convention, discovery mechanism, ordering, parallelism/serial).

## 3. Environment & Prerequisites
Document exactly:
- Required tools (cargo, docker, kind, kubectl, curl, jq, openssl, etc.) ONLY if the code/scripts check for them or call them.
- Required env vars and defaults (including ports, paths, feature flags, timeouts).
- OS assumptions (Linux/macOS), network assumptions, docker requirements.
- Any test data/fixtures required and where they live.

## 4. Lifecycle: How a Full E2E Run Works
Give a step-by-step timeline of:
- Setup phase (what gets built, what gets started, what resources created).
- Execution phase (how cases are selected and executed).
- Teardown phase (what gets cleaned up, what is preserved on failure).
Include:
- Exact commands used (copy from scripts; do not paraphrase shell commands).
- Where logs/artifacts go.
- What “success” vs “failure” means for the overall run.

## 5. Case-by-Case Specification (MOST IMPORTANT)
For EACH test case (each `case_*.sh` or equivalent), produce a subsection:

### 5.x <case name>
- **Purpose**: What scenario it verifies.
- **Inputs**:
  - Config files used (paths).
  - Requests sent (curl commands, endpoints, headers, payloads).
  - Env vars / flags affecting this case.
- **Steps**: Numbered steps, using the real commands from the case.
- **Expected outputs**:
  - HTTP status/body/headers expectations.
  - File outputs (paths + expected contents/patterns).
  - Log expectations (exact grep patterns).
  - Timing expectations (sleep/retry windows).
- **Failure modes**: what the case treats as failure (non-zero exit, mismatch, timeout).
- **Dependencies**: what must already be running for this case.

If a case calls shared helpers (e.g., `assert.sh`), inline the behavior: explain what the helper checks and how.

## 6. Coverage Matrix
Create a Markdown table:
- Rows: major feature areas (routing/match, LB, headers, TLS, reload, failure handling, etc.) — ONLY include areas that you can prove exist in cases.
- Columns: case names.
- Cells: brief notes on what each case covers.

## 7. Gaps & Risks (still evidence-based)
List:
- Scenarios that appear intended but are not covered by any case.
- Flaky patterns (sleep-based, race-prone).
- Hidden coupling (hard-coded ports, global state, shared temp dirs).
Each item must reference evidence (or explicitly label as inference).

# Extra requirements
- Include a “How to run locally” block that uses the exact documented/used commands found in the crate (don’t invent new CLI).
- If the crate supports multiple modes (local vs docker vs k8s), separate them and describe how mode selection works.
- Keep the report readable: use headings, code blocks, and concise bullets.
