[package]
name = "pavis-core"
version = "0.0.0"
edition = "2024"
license = "Apache-2.0"
description = "Core primitives and engine for the Pavis data plane"

[dependencies]
serde = { workspace = true }
rkyv = { version = "0.7", features = ["validation"] }
tracing = { workspace = true }