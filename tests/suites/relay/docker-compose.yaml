services:
  relay:
    image: pavis-relay:local
    profiles: ["sut"]
    entrypoint: ["/usr/local/bin/pavis-relay"]
    working_dir: /workspace
    volumes:
      - ../../..:/workspace
    environment:
      - RUST_LOG=debug
    command: ["--help"]
